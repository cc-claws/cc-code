# 代码卫生扫描报告

**扫描日期**：2026-06-27
**扫描范围**：全仓库（7 个 Workspace Crate + side-projects）
**技术栈**：Rust 2021, tokio async, ratatui TUI

---

## 与上次对比

上次：2026-05-26

| 维度 | 上次 | 本次 | 变化 |
|------|------|------|------|
| 大文件 (≥400行, 非测试) | 46 | 15 | -31 (拆分成功 + 口径修正) |
| 死代码 | 0 | 0 | — |
| 测试混合 | 0 | 0 | — |
| 架构问题 | 3 | 3 | — |
| **合计** | **49** | **18** | **-31** |

> 上次 46 个大文件含 test 文件和 side-projects。本次严格区分：非测试源文件 15 个 ≥400 行（其中 8 个 ≥800 行严重阈值）。过去 1 个月以 bug 修复和维护为主（npm 0.6.22 发版、MCP OAuth、mouse escape、Ctrl-C 等），无大规模特性涌入，但 `message_render.rs`（+641）和 `message_view/mod.rs`（+319）出现显著膨胀。成功拆分：`keyboard.rs` 1222→多文件、`plugin_panel/mod.rs` 827→拆分后消失、`anthropic/invoke.rs` 和 `openai/invoke.rs` 已拆。

---

## 总览

整体改善。上次报告中 4 个 🔴 紧急拆分目标中 2 个成功拆分（`keyboard.rs`、`plugin_panel/mod.rs`），但 `message_render.rs` 从 612 行暴涨至 1253 行成为新的最大文件。死代码和测试分离持续满分。

| 维度 | 评级 | 问题数 | 关键指标 |
|------|------|--------|---------|
| 大文件/上帝类 | 🔴 | 15 | 最大文件 1253 行，8 个 ≥800 行 |
| 死代码 | 🟢 | 0 | clippy 零 warning，TODO 仅 8 处（5 文件） |
| 测试与实现混合 | 🟢 | 0 | 全部 `#[cfg(test)] mod xxx_test;` 分离 |
| 架构坏味道 | 🟡 | 3 | app/mod.rs 65 子模块 |

---

## 大文件 Top 15（≥400 行，非测试源文件）

| # | 文件 | 行数 | 状态 | 建议 | 标记 |
|---|------|------|------|------|------|
| 1 | `peri-tui/src/ui/message_render.rs` | 1253 | 🔴 | 消息渲染+工具渲染+代码块混合，应拆出 tool/code 子模块 | **严重恶化** (+641) |
| 2 | `peri-tui/src/main.rs` | 1138 | 🔴 | CLI 参数解析+TUI 启动+信号处理混合 | 恶化 (+355) |
| 3 | `peri-tui/src/app/message_pipeline/mod.rs` | 1070 | 🔴 | 流式管线 47 个函数，chunk/throttle/subagent 可拆 | **恶化** (+291) |
| 4 | `peri-tui/src/ui/message_view/mod.rs` | 1033 | 🔴 | 消息视图+滚动+选区混合 | **恶化** (+319) |
| 5 | `peri-tui/src/event/mod.rs` | 1003 | 🔴 | 事件分发中心，key→action 映射膨胀 | 恶化 (+95) |
| 6 | `peri-tui/src/acp_stdio.rs` | 988 | 🔴 | Stdio 传输层，3 个函数承载全部逻辑 | 恶化 (+90) |
| 7 | `peri-tui/src/event/keyboard/normal_keys.rs` | 808 | 🔴 | keyboard.rs 拆分产物，仍偏大 | **新增** |
| 8 | `peri-middlewares/src/hooks/middleware.rs` | 783 | 🔴 | Hooks 中间件，matcher+executor+loader 混合 | 恶化 (+98) |
| 9 | `peri-acp/src/langfuse/tracer.rs` | 761 | 🟡 | 观测追踪器，on_trace/on_llm/on_tool 方法 | 已记录 |
| 10 | `peri-widgets/src/markdown/render_state.rs` | 748 | 🟡 | Markdown 渲染状态管理 | 已记录 |
| 11 | `peri-tui/src/clipboard/copy.rs` | 746 | 🟡 | 剪贴板复制逻辑 | **新增** |
| 12 | `peri-tui/src/ui/main_ui/status_bar.rs` | 680 | 🟡 | 状态栏渲染 | **新增** (跨阈值) |
| 13 | `peri-middlewares/src/plugin/loader.rs` | 669 | 🟡 | 插件加载器 | 已记录 |
| 14 | `peri-tui/src/app/mod.rs` | 668 | 🟡 | App 入口，65 个子模块 | 恶化 (+109) |
| 15 | `peri-middlewares/src/mcp/config.rs` | 654 | 🟡 | MCP 配置解析 | 已记录 |

### 测试文件 Top 5（仅参考，不计入问题）

| 文件 | 行数 | 备注 |
|------|------|------|
| `peri-tui/src/ui/headless_test.rs` | 3303 | +2400，持续膨胀 |
| `peri-tui/src/app/message_pipeline/message_pipeline_test.rs` | 2010 | +617 |
| `side-projects/git-graph/src/editor/mod.rs` | 1873 | 非 workspace 主线 |
| `peri-agent/src/agent/executor/mod_test.rs` | 1531 | +265 |
| `peri-middlewares/src/subagent/tool/tool_test.rs` | 1016 | 稳定 |

### side-projects 大文件（仅参考）

| 文件 | 行数 | 备注 |
|------|------|------|
| `side-projects/git-graph/src/editor/mod.rs` | 1873 | 🔴 严重超限 |
| `side-projects/git-graph/src/event.rs` | 1278 | 🔴 |
| `side-projects/git-graph/src/graph/layout.rs` | 1112 | 🔴 |
| `side-projects/git-graph/src/app.rs` | 730 | 🟡 |

---

## 死代码清单

| 位置 | 类型 | 名称 | 来源 | 状态 |
|------|------|------|------|------|
| — | — | — | — | — |

> `cargo clippy --workspace` 零 unused/dead_code warning。TODO/FIXME 仅 8 处（`core_tools.rs` 2 处、`tools/todo.rs` 2 处、`list_overlay_test.rs` 2 处、`event.rs` 1 处、`message_area.rs` 1 处）。无大规模注释掉的代码块。死代码维度持续满分。

---

## 内联测试清单

| 文件 | 测试行数 | 阈值 | 建议 | 标记 |
|------|---------|------|------|------|
| — | — | — | — | — |

> 项目持续遵循 `#[cfg(test)] mod xxx_test;` 模式。所有 `#[cfg(test)]` 均为模块导入存根（3-6 行），无内联测试代码。最大内联块为 `thread/types.rs`（43 行，< 100 行阈值，可接受）。符合 CLAUDE.md 规范。

---

## 架构问题清单

| 位置 | 问题 | 严重程度 | 标记 |
|------|------|---------|------|
| `peri-tui/src/app/mod.rs` | 65 个子模块（远超 15 阈值），通过 `.inc` include 文件组织 | 🟡 | 恶化 (61→65) |
| `peri-middlewares/src/subagent/mod.rs` | 6 个子模块 | 🟢 | 正常 |
| `peri-tui/src/ui/message_view/utils.rs` | utils 模块存在但内容合理（已审核） | 🟢 | 已记录 |

### 依赖方向验证

对照 CLAUDE.md 依赖关系图，所有 Cargo.toml 依赖方向正确：
- `peri-agent` → 无 workspace crate 依赖 ✓
- `peri-middlewares` → 依赖 `peri-agent` ✓
- `peri-acp` → 依赖 `peri-agent` + `peri-middlewares` + `peri-lsp` + `langfuse-client` ✓
- `peri-tui` → 依赖 `peri-agent` + `peri-middlewares` + `peri-acp` + `peri-lsp` + `peri-widgets` + `langfuse-client` ✓
- `peri-widgets` / `langfuse-client` / `peri-lsp` → 无 workspace crate 依赖 ✓

无万能模块（`utils.rs`/`common.rs`/`helpers.rs`/`misc.rs`）✓（除已审核的 `message_view/utils.rs`）

---

## 行动清单

### 本期紧急（≥1000 行，必须拆分）

- [ ] 🔴 拆分 `message_render.rs`（1253 行，+641，消息渲染+工具渲染+代码块渲染应分离） **[严重恶化]**
- [ ] 🔴 拆分 `main.rs`（1138 行，CLI 参数+TUI 启动+信号处理应分离） [恶化]
- [ ] 🔴 拆分 `message_pipeline/mod.rs`（1070 行，+291，47 个函数，chunk/throttle/subagent 三块可拆） **[恶化]**
- [ ] 🔴 拆分 `message_view/mod.rs`（1033 行，+319，消息视图+滚动+选区混合） **[恶化]**
- [ ] 🔴 拆分 `event/mod.rs`（1003 行，+95，事件分发中心持续膨胀） [恶化]

### 本期重点（800-1000 行）

- [ ] 🔴 拆分 `acp_stdio.rs`（988 行，+90，仅 3 个函数但 run_acp_stdio 单函数过长） [恶化]
- [ ] 🔴 拆分 `event/keyboard/normal_keys.rs`（808 行，keyboard.rs 拆分产物仍偏大） [新增]
- [ ] 🔴 拆分 `hooks/middleware.rs`（783 行，+98，matcher+executor+loader 可拆） [恶化]

### 新增关注

- [ ] 🟡 关注 `clipboard/copy.rs`（746 行，新增） [新增]
- [ ] 🟡 关注 `status_bar.rs`（680 行，新增，跨 400 行阈值） [新增]
- [ ] 🟡 关注 `app/mod.rs`（668 行，+109，65 子模块持续膨胀） [恶化]

### 已记录（持续推进）

- [ ] 🟡 拆分 `langfuse/tracer.rs`（761 行） [已记录]
- [ ] 🟡 拆分 `markdown/render_state.rs`（748 行） [已记录]
- [ ] 🟡 拆分 `plugin/loader.rs`（669 行） [已记录]
- [ ] 🟡 拆分 `mcp/config.rs`（654 行） [已记录]

### 上次行动项回顾

| 行动项 | 状态 |
|--------|------|
| 🔴 拆分 `subagent/tool/define.rs`（1242 行） | ✅ 已拆分（降至 ~630 行） |
| 🔴 拆分 `event/keyboard.rs`（1222 行） | ✅ 已拆分（拆为 normal_keys/panels 等） |
| 🔴 拆分 `event/mod.rs`（908 行） | ❌ 恶化至 1003 行 |
| 🔴 拆分 `acp_stdio.rs`（898 行） | ❌ 恶化至 988 行 |
| 🔴 拆分 `plugin_panel/mod.rs`（827 行） | ✅ 已拆分（降至 630 行） |
| 🔴 拆分 `langfuse/tracer.rs`（764 行） | ⚠️ 微降至 761 行 |
| 🟡 拆分 `anthropic/invoke.rs`（692 行） | ✅ 已拆分 |
| 🟡 拆分 `openai/invoke.rs`（664 行） | ✅ 已拆分 |
| 🔴 重组 `app/mod.rs`（61 子模块） | ❌ 恶化至 65 子模块 |
