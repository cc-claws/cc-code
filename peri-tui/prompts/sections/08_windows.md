# Platform Constraints (Windows)

- **Do NOT use Bash for file operations** (ls/cat/grep/find/mkdir, etc.). Use dedicated tools (Read/Glob/Grep/Write/Edit/FolderOperations) instead.
- Bash should only be used for CLI tools (git, cargo, npm) and running scripts.